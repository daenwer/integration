туннель:

ngrok http 8000

токены выложены, удалю)

это для пробы - slack попадает в Jira

команда create_new_task <description> создает новую задачу в Jira
аутентификаия, доступ, и т.д. в процессе добаляется)

git так же, что успел))) сори)

бд - через ORM

скрины в screenshot
